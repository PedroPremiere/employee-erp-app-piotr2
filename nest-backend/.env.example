APP_BASEURL=localhost
APP_PORT=3000
APP_SECRET=QWERTY
TOKEN_EXPIRES_IN=100s

#set to true for ci/cd
ISOLATED_MODULES=false

COMPOSE_PROJECT_NAME=erpsystem

DATABASE_PORT=3306
DATABASE_DIALECT=mysql
DATABASE_HOST=127.0.0.1
DATABASE_USERNAME=user
DATABASE_PASSWORD=password
DATABASE_NAME=demodb

FRONTEND_URL=http://localhost:8080

TEST_DATABASE_NAME=testdb
TEST_DATABASE_PORT=3307

EMAIL_HOST=127.0.0.1
EMAIL_PORT=1025
EMAIL_AUTH_USER=null
EMAIL_AUTH_PASSWORD=null
EMAIL_SECURE=false
EMAIL_FROM_ADDRESS=info@erp.local
EMAIL_FROM_NAME=erp.local


RABBITMQ_USER=user
RABBITMQ_PASS=password
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672
RABBITMQ_HOST=localhost
# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="mysql://user:password@127.0.0.1:3306/demodb?schema=public"


# You MUST set a secret to secure/pepper the stored user passwords here. Use at least 64 characters.
# Generate one by using for example: pwgen -N 1 -s 96
# ATTENTION: This value must be the same on all Graylog nodes in the cluster.
# Changing this value after installation will render all user sessions and encrypted values in the database invalid. (e.g. encrypted access tokens)
GRAYLOG_PASSWORD_SECRET=QZcbJpY75SLKYTacou44bekoUgHgllvT7fjLIXFp659QMbdEfDz9bCFuzJyjrxIfqnxWYzBv38kPFO1TyhYjmQmj32nz4rS3

# You MUST specify a hash password for the root user (which you only need to initially set up the
# system and in case you lose connectivity to your authentication backend)
# This password cannot be changed using the API or via the web interface. If you need to change it,
# modify it in this file.
# Create one by using for example: echo -n yourpassword | shasum -a 256
# and put the resulting hash value into the following line
# CHANGE THIS!
# password is  yourpassword
GRAYLOG_ROOT_PASSWORD_SHA2=e3c652f0ba0b4801205814f8b6bc49672c4c74e25b497770bb89b22cdeb4e951